/*let frutas = ["Apple","Manzana",45,"@sfq143"];
frutas.push("Pera")
frutas.pop()
alert( frutas.shift() )
frutas.unshift("Apple")
console.log(frutas);
console.log(frutas.length)*/

/*
let array = [2,4,7]

let suma = array.reduce((suma, actual) => suma + actual);

alert(suma)
*/

/*const numeros = [1,2,3,4,5,6]
const pares = []

let cuantosPares = 0;
let contadorPares = numeros.reduce((cuantosPares, numeroActual) =>{
    if(numeroActual%2 == 0){
        cuantosPares+= 1
        pares.push(numeroActual)
    }
})

alert(numeros)
alert(contadorPares)
alert(pares) */

