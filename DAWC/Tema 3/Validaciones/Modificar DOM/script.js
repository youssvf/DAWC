function contarElementos(elemento){
    alert(document.querySelectorAll(elemento).length);
}

function obtenerCaracteres(){
    const parrafo = document.querySelectorAll("p")[0];
    const texto = parrafo.textContent.slice(0,19);
    alert(texto);
}

function

// contarElementos("a");
obtenerCaracteres();

